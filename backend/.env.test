# Entorno de Testing
NODE_ENV=test

# Base de datos de testing
DATABASE_URL=postgresql://postgres:dev_password@localhost:5432/kontaflow_test

# Server
PORT=9000
HOST=0.0.0.0

# Logging (fatal = minimum logging)
LOG_LEVEL=fatal

# Clerk (mocked en tests)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=test_publishable_key
CLERK_SECRET_KEY=test_secret_key

# Redis (opcional para tests)
REDIS_URL=redis://localhost:6379/1

# MinIO (opcional para tests)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_USE_SSL=false

# SMTP (opcional para tests)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test
SMTP_PASSWORD=test
SMTP_FROM=noreply@kontaflow.test
